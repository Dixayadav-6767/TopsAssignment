#include<stdio.h>
int main() {
	int i,n;
	printf("Enter the number of start point:");
	scanf("%d", &i);
	printf("Enter the number of end point:");
	scanf("%d", &n);
	while(i <= n){
		printf("\n i = %d", i);
		i++;
		
	}
}
